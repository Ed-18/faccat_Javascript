let base, altura, area

base=parseFloat(prompt("sigite a base do retângulo: "));
altura=parseFloat(prompt("digite aqui o valor da altura: "));

area= base * altura

alert("a área do retângulo é de: " +area);